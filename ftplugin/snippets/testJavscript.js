import unnamedImport, { nameImport } from "../filepath"

describe("", () => {
  test("match equality", () => {
    expect([[something]].toEqual(value))
  })
  test("match exact equality", () => {
    expect([[something]].toBe(value))
  })
})
